{
  "name": "skye",
  "version": "1.0.0",
  "description": "A day/night sky clock app that speaks the time, with a stopwatch, timer, and calculator — wrapped as a native app with Capacitor.",
  "private": true,
  "scripts": {
    "sync": "cap sync",
    "open:android": "cap open android",
    "open:ios": "cap open ios"
  },
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "@capacitor/ios": "^8.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2"
  }
}
